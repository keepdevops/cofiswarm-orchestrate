# Runtime dependencies for the bundled Python sidecars:
#   - orchestration.mlx_coordinator.sidecar  (:3003 orchestrate modes)
#   - orchestration.rag.service via rag-ingest-server.py  (:8001 RAG embed/index)
#
# Pinned to the versions verified working in the mlx-env dev environment.
# NOTE: mlx / mlx-lm are Apple-Silicon (macOS/arm64) only — the orchestrate
# modes + the MLX RAG embedder require macOS on Apple Silicon. On other
# platforms the venv install of mlx will fail; the app still runs C++-only.
aiohttp==3.14.0
asyncpg==0.31.0
pydantic==2.13.4
structlog==25.5.0
prometheus-client==0.25.0
numpy==2.4.3
mlx==0.31.2
mlx-lm==0.31.3
